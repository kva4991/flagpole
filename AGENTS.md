# Полные правила работы с проектом Super_pommels_and_flag

Эти правила действуют для всего репозитория. Они предназначены для следующих AI-агентов и людей, продолжающих работу над проектом. Явная новая инструкция владельца важнее этого файла; принятое долговременное решение после этого фиксируется в документации проекта. §faststart

## Название, язык и стиль общения

- Публичное название проекта и BLE-имя задаются раздельно в `project_identity.json`; после изменения выполнить `npm.cmd run identity:generate` и `npm.cmd run catalog:generate`.
- Основной язык общения с владельцем и инженерной документации — русский. `README.md` остаётся английским, `README.ru.md` — русским; их структура по возможности синхронна.
- Сначала сообщать результат и важные ограничения, затем детали. Не перегружать владельца внутренними названиями инструментов.
- Не выдавать предположение, изображение продавца, успешную компиляцию или статический тест за физически проверенный факт.
- Если для завершения нужны данные владельца, перечислить конкретно: фото, ссылка, маркировка, размер, модель прибора или результат измерения.
- Не повторять уже отвергнутые владельцем вопросы и решения без новых доказательств.

## Первый вход в новую сессию

1. Выполнить `git status --short --branch`, определить фактические ветку, remote и наличие чужих незакоммиченных изменений.
2. Прочитать `docs/agent-fast-start.ru.md`.
3. Проверить `docs/current-implementation-status.ru.md` и относящийся к задаче раздел `docs/AUDIT_V061_RU.md`.
4. Найти связанные §-теги в `docs/dev/tag-map.md`; читать целевые документы, а не выгружать всё дерево без необходимости.
5. Перед архитектурным решением открыть `docs/architecture/decisions/README.ru.md`.
6. Перед изменением геометрии открыть `MEASUREMENTS_REQUIRED_RU.md` и связанные документы в `mechanical/docs/`.
7. На Windows прочитать `tools/README.ru.md`, затем **до первого запуска любой команды, которая прямо или через npm вызывает Python**, синхронизировать проект в `%USERPROFILE%\Documents\pesochnica\flagpole\worktree` и выполнять её только там. Пробный Python-запуск из исходного Git-worktree запрещён.

## Карта проекта и источники истины

- `README.md` и `README.ru.md` — короткий публичный обзор и точка входа, не хранилище длинных инженерных рассуждений.
- `docs/current-implementation-status.ru.md` — изменяемый снимок текущего состояния с разделением «реализовано», «проверено локально», «требует стенда» и «только план».
- `docs/architecture/decisions/` — последовательная история устойчивых решений. Принятый ADR не переписывается задним числом; замена оформляется следующим номером.
- `LICENSE.md` — карта областей лицензирования; неизменённые тексты лицензий находятся в `LICENSES/`, аппаратные изменения дополнительно фиксируются в `CHANGES.txt`.
- `docs/dev/tag-map.md` — реестр стабильных §-тегов. Новый тег добавляется только вместе с основным документом.
- `catalog/components.json` — источник карточек компонентов, материалов и расходников.
- `catalog/physical-components.json` — единственный редактируемый источник физических размеров и массы покупных деталей, статуса/источника каждого значения и списка недостающих замеров. Актуальная документация и медиа-описания ссылаются на `catalog/catalog.html#component-<ID>`, а не повторяют числа. §physicalcomponents1
- `catalog/drawings.json` — структурный реестр чертежей, превью, 3D-моделей и ссылок на их пояснения; подробный текст в JSON не хранится.
- `mechanical/cad_drafts/*.json`, перечисленные в `catalogPolicy.cadDraftSources`, — размерные источники CAD-заготовок, для которых производные файлы ещё намеренно не созданы. Они отображаются на сайте отдельным блоком с явным статусом, а будущий build123d-источник читает тот же JSON. §bearingadapter1 §lowercablestrain1
- `catalog/media-descriptions/<ID>.md` — единственный подробный источник назначения, состава, инженерной логики, допущений и обязательных синхронных правок конкретного чертежа либо 3D-модели. Сразу после статуса находится краткий критический контракт: что нельзя потерять, что запрещено и что проверить. Имя файла обязано совпадать с ID карточки. Перед моделированием или изменением медиа сначала прочитать критический контракт, затем весь файл; после изменения обновить оба слоя в той же правке. §mediarationale1 §mediacontract1
- `catalog/catalog.html` — сгенерированный файл. Его нельзя редактировать вручную.
- `catalog/images/SOURCES.md` — обязательная атрибуция внешних справочных изображений.
- `mechanical/generate_build123d_canonical_v076.py` — воспроизводимый источник 23 актуальных печатных деталей, STEP/STL, раскладок и модели #204. `mechanical/generate_models_v06.py` оставлен для старых справочных сцен и не запускается без прямого запроса владельца. §drawstatus73
- SVG в `electronics/` — редактируемые источники схем; одноимённые PNG — превью для каталога.
- `electronics/firmware/` — обе линии прошивки; не смешивать историческую v0.5 и прототип Crucian v0.6.
- `android/crucian-control/` — Android-проект; использовать закреплённый Gradle Wrapper.
- `.github/workflows/validate.yml` — независимая CI-сборка, но не доказательство поведения реального устройства.

## Рабочий порядок изменения

<!-- v076-cad-workflow:start -->
## Точный CAD-контур build123d

Для новых STEP/B-Rep деталей использовать только `build123d-mcp` и процессы `modeling`, `drawing`, `repair`. Полный порядок, пилотные ID и критерии принятия находятся в `mechanical/docs/BUILD123D_MCP_WORKFLOW_RU.md`. Внешняя MCP-сессия не является источником истины: итог сохраняется как воспроизводимый Python build123d. Не добавлять в корневые правила полный текст сторонних навыков и не подключать `text-to-cad`, `draftwright` или Blender-набор как обязательную зависимость без отдельного ADR.

build123d применяется к каноническим печатным PETG/TPU-деталям, их STEP/STL,
печатным раскладкам и основной разнесённой модели печатного комплекта. Справочные
2D-схемы и непечатные интерактивные сцены сохраняют прежние генераторы и не
переносятся на build123d автоматически. К ним относятся схемы электроники,
выкройка лямок, общий вид сборки, маршрут проводов, продольная схема, отдельный
лист светового тоннеля, компоновка электроники, шаблон рыбы и интерактивные
общий вид/компоновка электроники. Их перегенерация выполняется только по
прямому запросу владельца; изменение печатной геометрии само по себе не
разрешает запуск этих старых генераторов.
<!-- v076-cad-workflow:end -->

1. Собрать факты: целевой поиск через `rg`, чтение ближайших документов, проверка исходных данных и текущего diff. Для любой карточки чертежа/3D сначала определить её ID в `catalog/drawings.json`, прочитать блок `Критический контракт`, затем полностью прочитать `catalog/media-descriptions/<ID>.md`.
2. Определить затронутые границы: механика, электроника, прошивка, Android, каталог, документация, CI.
3. Изменять источник истины, а не только производный файл.
4. В той же сессии обновить тематический документ, текущий статус, ADR/§-тег и тест, если они затронуты.
5. Сначала выполнить узкую быструю проверку изменённой области, затем обязательные проверки перед публикацией.
6. Для полной регенерации использовать единый `build.cmd all` из защищённой копии; не поддерживать параллельный ручной список тех же команд в CI или README. §build01
7. Обновить `CHECKSUMS_SHA256.txt` только после завершения осмысленных правок.
8. Проверить `git diff --check`, `git status --short`, полный diff и перечень staged-файлов.
9. Индексировать только точные относящиеся к задаче пути. Не использовать `git add .`, `git add -A` или `git add --all`.
10. Коммит и push выполнять только в согласованную ветку и только когда это разрешено владельцем. Не смешивать посторонние изменения в коммит.

## Когда обязательно обновлять карточки каталога

Карточка в `catalog/components.json` и связанная запись в `catalog/physical-components.json` обновляются в той же сессии, если произошло хотя бы одно из событий:

- добавлен, удалён, заменён или объединён компонент, материал, расходник, инструмент или покупная сборка;
- выбран другой вариант продавца, модель, номинал, цвет, размер, материал, количество или электрический параметр;
- владелец прислал ссылку, фото, маркировку, описание товара либо уточнил, что деталь уже есть;
- появились фактические измерения или стендовая проверка, подтверждающие либо опровергающие данные продавца;
- изменилось назначение детали, место установки, способ подключения, монтажа, герметизации или обслуживания;
- изменилась основная схема, BOM, геометрия или принятое ADR и старая карточка стала вводить в заблуждение;
- ссылка перестала вести на нужный вариант, изображение оказалось неверным, текстовым или относится к другому изделию;
- изменились `missing`, `imageStatus`, категория, возможные названия либо статус «кандидат/выбран/подтверждён».

При обновлении карточек:

- ID компонентов — только строки `001`…`099`, без префиксов и пропусков; существующий ID сохраняется за той же логической позицией;
- новая позиция получает следующий последовательный ID; при осознанном удалении или объединении карточек перенумеровать все последующие позиции, ссылки и тестовые ожидания в том же коммите, не оставляя пропусков;
- сохранять только полезные технические сведения, рекламный текст отбрасывать;
- утверждения продавца явно отделять от измеренных и проверенных данных;
- размеры, массу и их происхождение редактировать только в `physical-components.json`; `components.json` не должен становиться вторым размерным реестром;
- в актуальном тексте вместо повторения физического размера давать ссылку на точную карточку `catalog/catalog.html#component-<ID>`; численные параметры печатной геометрии по-прежнему принадлежат CAD;
- фото реального экземпляра помечать `confirmed`, фото продавца — `listing`, внешнюю иллюстрацию — `reference`, временный эскиз — `placeholder`;
- не добавлять изображения, состоящие только из текста, таблицы или списка характеристик; полезный текст распознавать и переносить в `purpose`, `specifications` или `missing`;
- если своего фото нет, подобрать справочное изображение с понятной лицензией, сохранить локально и записать автора, источник, лицензию и ограничения в карточке и `catalog/images/SOURCES.md`;
- не выдавать справочную фотографию за конкретную купленную модель;
- после изменения JSON или изображений выполнить `npm.cmd run catalog:generate`, затем `npm.cmd run catalog:check`.

## Когда обязательно обновлять чертежи и 3D на странице

Чертежи, превью, модели и вкладка сайта обновляются в той же сессии, если:

- изменена геометрия, размер, посадка, зазор, толщина стенки, отверстие, паз, крепёж или положение узла;
- изменены маршруты проводов, контакты, напряжения, распиновка, состав схемы или электрические соединения;
- изменено положение ESP32, VEML7700, AHT20+BMP280, MOSFET, DC-DC, токосъёмника, подшипников, спицы, мембраны или окна светового колодца;
- изменён материал так, что это влияет на геометрию, усадку, способ сборки либо маркировку раскладки печати;
- создан, удалён, переименован или перемещён SVG, PNG, STL, GLB либо файл превью;
- новая модель или чертёж стали полезны для сборки, печати, проверки или передачи следующему агенту;
- старое превью больше не соответствует исходнику, а подробное описание, объяснение инженерного решения или версия карточки стали устаревшими;
- изменилось понимание того, почему элемент расположен, ориентирован, разделён, упрощён или показан именно так, даже если геометрия пока не менялась.

Правила синхронизации:

1. До правки определить ID карточки и полностью прочитать `catalog/media-descriptions/<ID>.md`; при затрагивании нескольких карточек прочитать каждый связанный файл.
2. Менять первичный источник: генератор/параметры механики или SVG схемы.
3. Перегенерировать зависимые STL, GLB, PNG, отчёты и диагностические данные подходящим проектным скриптом.
4. В той же правке обновить `catalog/media-descriptions/<ID>.md`: назначение, состав вида, причины геометрии/ракурса, feature-ID, порядок сборки и обслуживания, предварительные допущения, список синхронных файлов и проверок. Даже неизменившийся путь не освобождает от обновления пояснения, если изменился смысл медиа.
5. Если путь, заголовок, категория, poster/preview, состав списка или ссылка `descriptionFile` изменились — обновить `catalog/drawings.json`. Поле `description` в этом JSON запрещено: подробный текст хранится только в ID-файле.
6. Если владелец запросил заготовку без генерации, не создавать пустые или фиктивные STL/STEP/GLB/PNG. Добавить размерный JSON и воспроизводимый исходник с явным opt-in запуском, включить JSON в `catalogPolicy.cadDraftSources` и показать карточку как `source-only-not-generated`. После реальной генерации удалить этот статус и провести обычную синхронизацию производных файлов.
7. Если содержимое медиа изменилось при прежнем имени, `drawings.json` можно не менять, но новый PNG/GLB/SVG и обновлённый ID-файл обязаны попасть в ту же правку.
8. Выполнить `npm.cmd run media:descriptions:check`, затем `npm.cmd run catalog:generate` и `npm.cmd run catalog:check`.
8. Проверить локальную страницу командой из `catalog/README_RU.md`. Для GLB использовать HTTP-сервер, а не считать просмотр через `file://` достаточным.
9. При сильном кэшировании проверить `Ctrl+F5` или новый профиль браузера; старое изображение из кэша не является доказательством рассинхронизации репозитория.

ID чертежей занимают диапазон `101`…`199`, ID 3D-моделей — `201`…`299`. Все ID являются трёхзначными строками без буквенных префиксов и должны быть уникальны среди видимых объектов каталога.

<!-- v075-catalog-policy:start -->
### Обязательная политика публикации чертежей и 3D

<!-- v076-media-presentation:start -->
### Представление ID и печатных раскладок v0.7.6

- миниатюры 2D/3D всегда чистые, без ID;
- полноразмерная 2D-копия содержит автоматически размещённые близкие выноски;
- hotspots скрыты в карточке и доступны в полноэкранном viewer, где кнопка `ID` рядом с крестиком переключает их видимость;
- раскладки уплотняются `mechanical/layout_packing.py` с проверяемым зазором;
- канонических жертвенных PETG-подложек под TPU нет; brim/raft задаётся слайсером;
- решения закреплены ADR-0027, ADR-0028 и ADR-0029.
<!-- v076-media-presentation:end -->

- Рабочий `catalog/drawings.json` содержит только `status: current` и только версию из `VERSION.txt`; исторические файлы сохраняются вне рабочего списка.
- У каждой текущей записи обязателен `descriptionFile`, ведущий ровно на один `catalog/media-descriptions/<ID>.md`. ID во front matter и имя файла обязаны совпадать с ID карточки; каждый файл начинается с проверяемого критического контракта; сиротские файлы и повторяющее поле `description` в JSON запрещены. §mediarationale1 §mediacontract1
- Каждый текущий чертёж и каждая текущая 3D-модель обязаны иметь ID-выноски из реестров компонентов/печатных деталей.
- Единственное исключение — `kind: print-layout`: у раскладки обязателен `calloutMode: exempt`, причина исключения и отсутствие `callouts`.
- Для 2D сохранять чистый исходник и генерировать подписанную PNG-копию; для GLB использовать hotspots, а не текстовую геометрию.
- Презентационная трансформация не создаёт второй экземпляр логической детали. Семантический состав GLB проверять отдельным валидатором.
- Полное решение и команды зафиксированы в `docs/CATALOG_MEDIA_POLICY_RU.md` и ADR-0026.
<!-- v075-catalog-policy:end -->


## Что именно означает «обновить сайт»

- Локальная страница статическая и генерируется из JSON-реестров компонентов, физических размеров, медиа и перечисленных CAD-заготовок. Изменение исходных данных без перегенерации `catalog/catalog.html` считается незавершённым.
- Изменение SVG/PNG/GLB по прежнему пути становится видимым после обновления браузера; добавление либо переименование файла требует изменения `catalog/drawings.json` и генерации HTML.
- GitHub показывает закоммиченные файлы, но отдельный GitHub Pages-сайт пока не настроен. Не обещать автоматическую интернет-публикацию без соответствующего workflow и настройки Pages.
- Перед push проверять, что JSON, HTML, изображения, чертежи, модели и контрольные суммы относятся к одной версии.

## Документация и неочевидные находки

- Не оставлять важное решение только в чате, комментарии к коду или сообщении коммита. Даже небольшое принятое уточнение размера, формы, материала, места монтажа, запрета или проверки сразу переносить в профильный источник истины и тематический документ; устойчивый выбор дополнительно фиксировать в ADR либо сводке решений.
- Длинное актуальное объяснение хранить в тематическом документе; README должен оставаться понятным неспециалисту.
- При изменении поведения, схемы, протокола, материала, сборки или проверки обновлять документацию в той же сессии.
- Новый устойчивый выбор оформлять ADR. При замене решения создавать следующий ADR и отмечать, что он заменяет прежний; исторический текст не переписывать так, будто нового решения придерживались всегда.
- Неочевидную ловушку добавлять в ближайший тематический документ и связывать §-тегом, если она образует долговременный контракт.
- `docs/current-implementation-status.ru.md` обновлять при изменении числа карточек, состояния сборки, реализованного поведения или перечня обязательных физических проверок.
- Новую команду, которую должен выполнять пользователь, отражать в соответствующем README и проверять на существование.

## Лицензирование

- Собственные аппаратные исходники, включая CAD-код, схемы, чертежи, STL/GLB, шаблоны, BOM и аппаратную документацию, покрываются `CERN-OHL-S-2.0`.
- Собственные Android/firmware-исходники, CI, тесты и общие инструменты покрываются MIT, если файл не содержит другого уведомления.
- Сторонние изображения, библиотеки, Gradle Wrapper, datasheet-фрагменты и иные внешние материалы не перелицензировать; сохранять исходные уведомления и атрибуцию.
- Для нового типа файла или новой папки сразу определить область в `LICENSE.md`; аппаратное изменение отметить в `CHANGES.txt` либо эквивалентном датированном уведомлении.
- Не удалять и не заменять существующий SPDX/copyright стороннего файла. Не добавлять SPDX проекта к файлу, права на который не принадлежат владельцу.
- Изменение варианта лицензии, границ покрытия или Source Location требует нового решения владельца и ADR. §license01

## Инженерные границы проекта

- Не менять размеры посадок древка, M125, подшипников и углепластиковой спицы без фактических измерений. §meas001
- Не считать токосъёмник несущей опорой и не проводить спицу через центральную ось. §mech001
- Текущая сборка печатается из уже купленного оранжевого PETG. ASA — только возможная будущая замена наружных жёстких деталей после появления бюджета и повторной проверки всех посадочных купонов; TPU не определяет соосность подшипников. §mat0001
- BLE-имя `Crucian` и часовое сервисное окно после настоящего включения питания являются принятыми решениями. BLE не должно снова открываться после Deep-sleep. §ble0001
- Текущий PIN не возвращается через GATT. `123456` — публичный заводской код первого подключения, а не секрет пользователя. §blesec1
- После устойчивого отказа VEML7700 подсветка переводится в безопасное выключенное состояние. §fwfail1
- Комбинированный AHT20+BMP280 питается от 3,3 В; климатический отказ не управляет подсветкой. §env001
- Карман климатического датчика располагается снизу; прямой наветренный продув и готовые малые отверстия в STL не допускаются, применяется послепечатное сверление по утоньшениям. §envmech1
- Окно светового колодца VEML7700 вырезается из фактически имеющегося прозрачного куска; его толщину нельзя закладывать без измерения, а датчик калибруется после установки окна. §lightwell1
- Два провода питания флага проходят по открытой обслуживаемой дорожке ниже крепежа, через наружную низкую точку и направляющую около 35°, а в электронный бокс входят только через разрезную манжету `#tpu95-3` / `#tpu95-4`; закрытый канал и отдельное отверстие над спицей не возвращать без нового ADR. §flagpower1
- Идентификаторы печатных деталей `#petg-*`, `#tpu95-*`, `#tpu85-*` стабильны между чертежами, таблицей и STL-реестром; при изменении состава обновлять реестр, ID-превью, таблицу, `drawings.json` и тесты в одном коммите. §partid1
- Сервисная крышка, VEML7700 и AHT20+BMP280 используют независимые обслуживаемые барьеры; активные зоны окна и мембран не покрывать герметиком, а IP-класс не заявлять до стенда. §weather73
- Верхняя лямка начинается на 10 мм ниже нижней кромки навершия, нижняя заканчивается вровень с низом флага, две средние распределяются равномерно; окончательную длину определять по одной пробной петле. §flagloop73
- Для изготовления использовать только карточки `current`. Рабочий каталог v0.7.6 вообще не публикует исторические карточки; архивные материалы служат только для изучения истории. Текущая механика полностью параметрическая, архивные сетки v0.7.3 не использовать как источник. §drawstatus73
- Отдельная вкладка «Новый CAD» и карточка #211 удалены после принятия полной build123d-линии. Каноническая #204 обязана содержать 23 native build123d-узла и 0 legacy mesh-узлов. Поле `experimentalModels` остаётся пустым только для совместимости схемы JSON. §cadworkflow
- Флаг шьётся УФ-стойкой bonded-полиэстеровой нитью; Tex 45 — исходный выбор для Oxford 210D, Tex 70 назначается только после пробы машины и пакета ткани. §flagsew1
- Проекту достаточно знать, что внешний аккумуляторный блок содержит 6×18650 и выдаёт стабилизированные 12 В. Проектирование BMS, зарядки и внутренней защиты блока находится вне Crucian. §power01
- Не заявлять IP-рейтинг, безопасный тепловой режим, долговечность на улице или аппаратную работоспособность без соответствующего физического испытания. §status1

## Проверки по области

- Идентичность проекта после изменения `project_identity.json`: `npm.cmd run identity:generate`, затем `npm.cmd run identity:check`.
- Документы: `npm.cmd run quality:docs:all`.
- Пояснения к чертежам/3D: `npm.cmd run media:descriptions:check`.
- Каталог после любого изменения карточек, изображений, чертежей или моделей: `npm.cmd run catalog:check`.
- Локальные контракты: сначала целевой `node --test <файл-теста>`, перед push — `npm.cmd test`.
- Карта влияния: `npm.cmd run review:impact`; каждую предложенную ручную проверку либо выполнить, либо явно назвать невыполненной.
- Контрольные суммы в самом конце: `npm.cmd run checksums:update`, затем `npm.cmd run checksums:check`.
- Полный локальный документальный gate: `npm.cmd run quality:gate`.
- Прошивка: PlatformIO build соответствующего проекта, затем реальный стенд BLE/PWM/deep-sleep/датчиков.
- Android: `android\crucian-control\gradlew.bat -p android\crucian-control :app:assembleDebug`, затем реальный телефон и реальное BLE-устройство.
- Механика: через защищённую копию выполнить `python mechanical/generate_build123d_canonical_v076.py`, целевые render-скрипты build123d и `python mechanical/validate_models_v06.py`. Старый `generate_models_v06.py` допускается только в явном режиме `legacy-references`; обычное изменение печатной детали не разрешает обновлять справочные сцены.
- Схемы: проверить SVG и PNG, сверить BOM, распиновку и фактическую маркировку модулей.

Зелёная проверка подтверждает только тот слой, который она действительно выполнила. Она не заменяет печать, измерение, полив, тепловой тест, прошивку платы, BLE-сопряжение или электрический стенд.

## Windows, антивирус и артефакты

- Использовать `tools/windows/setup.ps1` и `tools/windows/check.ps1`; не устанавливать дублирующие toolchain без доказанной необходимости.
- Для точного CAD использовать `tools/windows/setup-cad.ps1`: FreeCAD/OpenSCAD устанавливаются через winget в стандартные папки Windows, build123d/OCP — в `pesochnica\flagpole\.venv-build123d`. Не устанавливать build123d/OCP в глобальный Python. §cadworkflow
- Исполняемая и сборочная зона на компьютере владельца — `%USERPROFILE%\Documents\pesochnica\flagpole`. Антивирусное исключение должно охватывать только эту папку проекта.
- Исходный Git-репозиторий остаётся источником правок, но не местом исполнения Python. Любую команду, которая прямо или через npm/Node вызывает Python, сразу запускать только из синхронизированной копии `pesochnica\flagpole\worktree` способом, описанным в `tools/README.ru.md`; не делать предварительную попытку в исходном worktree.
- Ошибка `spawnSync py EPERM` вне `pesochnica` означает ограничение места исполнения, а не дефект проекта. Не повторять ту же команду там: синхронизировать актуальное дерево в защищённую зону и продолжить в ней. Чистые Node-проверки, заведомо не запускающие Python, разрешены в исходном Git-worktree.
- APK, BIN, PlatformIO output, Gradle cache, скачанные SDK и временные скрипты не коммитить. Явно заказанные артефакты складывать в `pesochnica\flagpole\artifacts`.
- Все STL и GLB индексировать через Git LFS; PNG оставлять в обычном Git. Не выполнять переписывание опубликованной истории и не включать LFS во все CI jobs без отдельного решения. §build01
- Не отключать антивирус, не маскировать и не обфусцировать скрипты ради обхода защиты.

## Git и чистота репозитория

- Сохранять пользовательские и чужие изменения; не применять `git reset --hard`, `git checkout --` или массовое удаление без явного разрешения.
- Не коммитить секреты, токены, личные данные, абсолютные пользовательские пути, build/cache-каталоги, APK/BIN, архивы передачи и временные файлы.
- Не менять принятую схему лицензирования и не перелицензировать существующие файлы без отдельного решения владельца.
- Перед коммитом проверить staged diff и убедиться, что в нём только файлы текущей задачи.
- После commit/push проверить `git status --short` и фактический SHA; не заявлять публикацию до успешного ответа remote.
- Изменённые рабочие файлы синхронизировать в защищённую копию проекта, если текущий процесс владельца требует работу из `pesochnica`.

## Чек-лист завершения задачи

- Источник истины изменён, производные файлы синхронизированы.
- Карточки материалов/компонентов обновлены, если решение затронуло BOM, параметры, изображения или фактические данные.
- Чертежи, превью, GLB и `drawings.json` обновлены, если решение затронуло геометрию, схему, размещение либо состав файлов.
- `catalog/catalog.html` перегенерирован при изменении JSON, а локальная страница проверена по затронутой вкладке.
- Тематическая документация, статус, ADR, §-теги и тесты согласованы.
- Проверки выполнены пропорционально риску; аппаратно непроверенное названо непроверенным.
- Контрольные суммы обновлены последними.
- Diff чист от случайных файлов; точные пути проиндексированы; commit/push подтверждены только после успеха.
